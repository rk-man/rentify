const User = require("./../models/userModal");
const Interest = require("./../models/propertyModal");
const Like = require("./../models/likeModal");

const catchAsync = require("express-async-handler");
const appError = require("./../utils/appError");
